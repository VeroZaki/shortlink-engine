# Copy this file to .env and set values for your environment.
# Do not commit .env (it is in .gitignore).

# ----- Database (PostgreSQL) -----
# PGUSER=postgres
# PGPASSWORD=
# PGHOST=localhost

# On macOS with Homebrew PostgreSQL, often your system user works with no password:
# PGUSER=your_username
# PGPASSWORD=
# PGHOST=localhost

# ----- Application -----
# Base URL for generated short links (default: http://localhost:3000)
# SHORTLINK_BASE_URL=http://localhost:3000

# ----- Optional -----
# RAILS_MAX_THREADS=5
